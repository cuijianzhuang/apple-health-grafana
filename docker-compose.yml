services:
  grafana:
    build: ./grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-health}
      - GF_DEFAULT_LANGUAGE=zh-Hans
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    depends_on:
      - influx

  influx:
    image: influxdb:1.8
    restart: unless-stopped
    environment:
      - INFLUXDB_DB=${INFLUX_DB:-health}
    volumes:
      - influx-data:/var/lib/influxdb

  # 一次性 zip 导入（按需手动运行）：
  #   docker compose run --rm -v /path/to/export.zip:/export.zip ingester
  ingester:
    build: ./ingester
    profiles: ["import"]
    depends_on:
      - influx

  api:
    build: ./ingester
    restart: unless-stopped
    command: ["api.py"]
    ports:
      - "${API_PORT:-5353}:${API_PORT:-5353}"
    environment:
      - INFLUX_HOST=influx
      - INFLUX_DB=${INFLUX_DB:-health}
      - API_PORT=${API_PORT:-5353}
      - API_KEY=${API_KEY:-}
    depends_on:
      - influx

volumes:
  influx-data:
